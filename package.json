{
  "name": "mc-web-app",
  "version": "0.1.0",
  "private": true,
  "description": "Medicine Cloud — note reader & editor",
  "scripts": {
    "build": "node build.js",
    "dev": "npx serve src --listen 4000 --no-clipboard",
    "deploy:worker": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^0.0.9",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
